# 禅道 MCP 配置示例。复制为 .env 并填入真实值（.env 不会被提交）。
# 也可改在 mcp.json 的 env 中提供；同名变量「环境/ mcp.json」优先于 .env。
ZENTAO_URL='http://your-zentao-host:8081'
ZENTAO_ACCOUNT='your-account'
ZENTAO_PASSWORD='your-password'
